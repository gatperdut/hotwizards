name: Build and deploy Hot Wizards Frontend

on:
  push:
    branches:
      - master
    paths:
      - "hwfe/**"
      - "shared/**"
      - "package.json"
      - "package-lock.json"
  workflow_dispatch:

jobs:
  build-and-deploy-hwfe:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Setup node.js
        uses: actions/setup-node@v6.3.0
        with:
          node-version: "24"
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Inject Sentry DSN
        working-directory: hwfe
        run: |
          sed -i "s|HWFE_SENTRY_DSN|${{ vars.HWFE_SENTRY_DSN }}|" src/environments/environment.ts

      - name: Build
        run: npm run build -w @hw/hwfe

      - name: Upload sourcemaps
        working-directory: hwfe
        run: |
          npx @sentry/cli releases new -p $SENTRY_PROJECT $GITHUB_SHA
          npx @sentry/cli releases upload-sourcemaps dist/hwfe/browser \
            --project $SENTRY_PROJECT \
            --release $GITHUB_SHA \
            --rewrite
          npx @sentry/cli releases finalize $GITHUB_SHA
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: "hot-wizards"
          SENTRY_PROJECT: "hwfe"

      - name: Cleanup
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ vars.SSH_HOST }}
          username: ${{ vars.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: rm -rf /var/www/hwfe/*

      - name: Deploy
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ vars.SSH_HOST }}
          username: ${{ vars.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "hwfe/dist/hwfe/browser/*"
          rm: true 
          target: "/var/www/hwfe"
          strip_components: 4

      - name: Generate sitemap
        working-directory: hwfe
        run: npx sitemap-generator-cli https://hotwizards.net --output dist/hwfe/browser/sitemap.xml