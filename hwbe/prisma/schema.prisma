generator client {
  provider = "prisma-client-js"
  output   = "../../prismagen/generated"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int      @id @default(autoincrement())
  handle    String   @unique
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}